using UnityEngine;

public class RumbleTest : MonoBehaviour
{    
    void Update()
    {
        //if (InputManager.instance.controls.Menus.Cancel.WasPressedThisFrame())
        {
            // Start the rumble
            //RumbleManager.Instance.RumblePulse(0.25f, 1f, 0.25f);
        }
    }
}
